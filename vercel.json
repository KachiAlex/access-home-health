{
  "version": 2,
  "buildCommand": "npm run build --workspace web",
  "installCommand": "npm ci --workspace web",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/apps/api/api/:path*"
    }
  ],
  "functions": {
    "apps/api/api/**/*.ts": {
      "runtime": "nodejs18.x",
      "memory": 1024,
      "maxDuration": 30
    }
  }
}
