{
  "name": "functions",
  "description": "Firebase Cloud Functions for Access Health",
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^7.0.5",
    "@sendgrid/mail": "^7.0.0",
    "@google-cloud/firestore": "^6.8.0",
    "@google-cloud/functions-framework": "^3.1.0"
  }
  ,
  "scripts": {
    "start": "npx @google-cloud/functions-framework --target=sendOrderEmailHttp --signature-type=http --port=$PORT"
  }
}
