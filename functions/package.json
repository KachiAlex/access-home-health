{
  "name": "functions",
  "description": "Firebase Cloud Functions for Access Health",
  "engines": {
    "node": "22"
  },
  "dependencies": {
    "firebase-admin": "^11.11.1",
    "firebase-functions": "^5.1.0"
  },
  "scripts": {
    "start": "npx @google-cloud/functions-framework --target=sendContact --signature-type=http --host=127.0.0.1"
  }
}
